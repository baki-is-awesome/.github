name: Continuous Delivery ArgoCD

on:
  push:
    branches:
      - $default-branch

jobs:
  say-hi:
    name: Say Hi!
    runs-on: ubuntu-latest
    steps:
      - name: Echo 'Say Hi!' 
        run: |
          echo "Say Hi!"
 
